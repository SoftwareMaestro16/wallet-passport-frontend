# @telegram-apps/telegram-ui's peer range is React 18; this app runs React 19 (verified working
# via local build + browser QA). Without this, a clean `npm install` (e.g. Vercel's build) fails
# with ERESOLVE instead of just warning, since npm 7+ treats peer conflicts as errors by default.
legacy-peer-deps=true
