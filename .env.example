# Base URL of the backend API (Fastify server, see ../server). Server routes are mounted at
# root (/auth/..., /passports/..., /health) — no /api prefix, don't add one here.
VITE_API_BASE_URL=https://walletpassport-185-163-47-190.sslip.io
