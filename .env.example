# Base URL of the backend API (Fastify server, see ../server).
# Placeholder points at the sslip.io box used during early development —
# actual backend routing/domain is still TBD, update once known.
VITE_API_BASE_URL=https://walletpassport-185-163-47-190.sslip.io/api
